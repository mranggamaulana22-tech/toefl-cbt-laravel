# Authenticating requests

To authenticate requests, include an **`Authorization`** header with the value **`"Bearer Bearer {YOUR_AUTH_TOKEN}"`**.

All authenticated endpoints are marked with a `requires authentication` badge in the documentation below.

Use a Bearer token in the `Authorization` header. Example: <code>Authorization: Bearer {your_token}</code>. You can create tokens in your account settings.
